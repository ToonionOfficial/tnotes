[workspace]
members = ["crates/tnotes-core", "apps/server", "apps/desktop", "crates/tnotes-document"]
resolver = "3"

[profile.release]
strip = true
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
