[package]
name = "tnotes-desktop"
version = "0.1.0"
edition = "2024"

[dependencies]
tnotes-core = { path = "../../crates/tnotes-core" }

gpui = "0.2"
twrite = "0.10.0"
rust-embed = "8.5"

[dev-dependencies]
gpui = { version = "0.2", features = ["test-support"] }
