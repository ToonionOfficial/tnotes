pub struct Theme {
    pub id: String,
    pub name: String,
    pub builtin: bool,
    pub schema: String,
    pub version: u64,
    pub updated_at: i64,
    pub created_at: i64,
    pub device_id: String,
}
